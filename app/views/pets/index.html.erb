<h1>All Pets</h1>
<% @pets.each do |pet| %>
    <h2><%= pet.name %></h2>
    <p>Breed: <%= pet.breed %></p>
    <p>Image: <%= pet.image %></p>
    <%= link_to "Pet Bio", pet %>
    <%= link_to "Edit pet", edit_pet_url, pet %>
    <% end %>


    <%= link_to "New Pet", new_pet_path %>